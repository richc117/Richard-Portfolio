---
layout: layouts/base.njk
---
<article class="essay">
  <header class="essay-header">
    <h1>{{ title }}</h1>
    <p class="meta">{{ date | formatDate }}{% if form %} · {{ form }}{% endif %}</p>
  </header>
  {{ content | safe }}
</article>
